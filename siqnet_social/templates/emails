<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>New Comment Alert</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: auto;
      background: white;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
    }
    .header {
      background-color: #00B894;
      color: white;
      padding: 20px;
      border-radius: 8px 8px 0 0;
      text-align: center;
    }
    .content {
      padding: 20px;
    }
    .button {
      background-color: #00B894;
      color: white;
      padding: 10px 15px;
      text-decoration: none;
      border-radius: 5px;
      display: inline-block;
      margin-top: 20px;
    }
    .footer {
      font-size: 12px;
      color: #888;
      text-align: center;
      margin-top: 30px;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h2>New Comment on Your Post</h2>
    </div>
    <div class="content">
      <p>Hi {{ user.username }},</p>
      <p>Someone just commented on your post titled <strong>{{ post_title }}</strong>.</p>
      <p>Click below to view the comment:</p>
      <p><a href="{{ comment_link }}" class="button">View Comment</a></p>
    </div>
    <div class="footer">
      &copy; {{ current_year }} SIQNet. All rights reserved.
    </div>
  </div>
</body>
</html>
